# Learn GitHub — Lesok + Grok

Первый учебный репозиторий.

## Что это
GitHub = облако для кода и файлов проектов.  
Git = история изменений на компьютере.

## Команды дня 1
```bash
export PATH="$HOME/.local/bin:$PATH"
gh auth login
git status
git add .
git commit -m "первый коммит"
gh repo create learn-github --public --source=. --remote=origin --push
```

## Урок
Grok + Lesok · 2026
